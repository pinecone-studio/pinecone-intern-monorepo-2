query GetBookingsByRoomId($roomId: ID!) {
  getBookingsByRoomId(roomId: $roomId) {
    id
    userId
    hotelId
    roomId
    checkInDate
    checkOutDate
    adults
    children
    status
    createdAt
    updatedAt
  }
}
