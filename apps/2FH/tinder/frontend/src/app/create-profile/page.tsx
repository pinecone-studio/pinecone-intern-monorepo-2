'use client';

import { SignupProvider, SignupContainer } from '@/components/profile';

const Page = () => {
  return (
    <SignupProvider>
      <SignupContainer />
    </SignupProvider>
  );
};

export default Page;
