'use client';

import { TinderProfile } from '@/components/profile/profile-settings';

const ProfileSettingsPage = () => {
  return <TinderProfile />;
};

ProfileSettingsPage.displayName = 'ProfileSettingsPage';

export default ProfileSettingsPage;
