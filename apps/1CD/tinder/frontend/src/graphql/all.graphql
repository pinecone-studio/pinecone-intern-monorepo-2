query Query {
  sampleQuery
}

mutation Mutation {
  sampleMutation
}

mutation updateAttraction($email: String!, $attraction: String!) {
  updateAttraction(email: $email, attraction: $attraction) {
    email
    attraction
  }
}
