query GetOrder {
  getOrder {
    _id
    createdAt
    status
    ticketType {
      _id
      additional
      discount
      soldQuantity
      unitPrice
      zoneName
    }
  }
}

mutation CancelTicket($input: CancelTicketTypeInput!) {
  cancelTicket(input: $input) {
    message
  }
}
