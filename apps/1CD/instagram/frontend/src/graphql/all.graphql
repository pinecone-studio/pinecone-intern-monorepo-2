query Query {
  sampleQuery
}

mutation Mutation {
  sampleMutation
}

mutation Signup($input: SignupInput!) {
  signup(input: $input) {
    user {
      _id
      userName
      fullName
      email
    }
    token
  }
}
