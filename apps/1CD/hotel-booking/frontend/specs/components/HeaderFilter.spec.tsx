import HeaderFilter from '@/components/search-hotel/HeaderFilter';
import { render } from '@testing-library/react';

describe('Header filter component', () => {
  it('it should render', () => {
    render(<HeaderFilter />);
  });
});
