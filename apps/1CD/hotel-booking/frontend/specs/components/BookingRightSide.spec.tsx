import { BookingPageRightSide } from '@/components/BookingPageRightSide';
import { render } from '@testing-library/react';

describe('Booking-Right-Side', () => {
  it('should be render', () => {
    render(<BookingPageRightSide />);
  });
});
